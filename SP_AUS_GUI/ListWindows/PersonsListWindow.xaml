<Window x:Class="SP_AUS_GUI.ListWindows.PersonsListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SP_AUS_GUI.ListWindows"
        mc:Ignorable="d"
        Title="Osoby" Height="450" Width="800">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Name="TextBlockDescription" FontWeight="Bold" Text=""></TextBlock>
        <DataGrid DockPanel.Dock="Bottom" x:Name="PersonsListGrid"
          AutoGenerateColumns="False"
          ItemsSource="{Binding Persons}"
          SelectionMode="Single"
          MouseDoubleClick="PersonsListGrid_MouseDoubleClick"
          IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=FirstName}" Header="Krstné meno"/>
                <DataGridTextColumn Binding="{Binding Path=LastName}" Header="Priezvisko"/>
                <DataGridTextColumn Binding="{Binding Path=DateOfBirth}" Header="Dátum narodenia"/>
                <DataGridTextColumn Binding="{Binding Path=PatientNumber}" Header="Číslo pacienta"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
